interface:
  display_name: "Harness Verify"
  short_description: "Verify a harness issue independently"
  default_prompt: "Use $verify to independently verify an in-review harness issue."
policy:
  allow_implicit_invocation: false
