interface:
  display_name: "Harness Status"
  short_description: "Show the current harness tracker status"
  default_prompt: "Use $status to clock in and show the current harness tracker status."
policy:
  allow_implicit_invocation: false
